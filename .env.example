# =============================================================================
# THE CORE AGENCY — Environment Variables
# Phase 2 (Database Schema V1) — 2026-06-07
#
# INSTRUCTIONS:
#   1. Copy this file to .env.local (NOT .env)
#   2. Fill in real values from your Supabase project dashboard
#   3. NEVER commit .env.local or any file with real secrets
#   4. .env.local is already in .gitignore
#
# RULE: Do not hardcode any value from this file in source code.
# =============================================================================

# --------------------------------------------------------
# SUPABASE (Phase 2+)
# Get from: Supabase Dashboard → Settings → API
# --------------------------------------------------------
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Server-side only (NOT prefixed with VITE_ — never expose to browser)
SUPABASE_SERVICE_ROLE_KEY=do_not_commit_real_secret

# --------------------------------------------------------
# DATABASE (Phase 2+)
# Get from: Supabase Dashboard → Settings → Database → Connection string
# --------------------------------------------------------
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project-id.supabase.co:5432/postgres

# --------------------------------------------------------
# WEBHOOK SECURITY (Phase 16+)
# Generate a strong random secret: openssl rand -hex 32
# --------------------------------------------------------
WEBHOOK_SHARED_SECRET=do_not_commit_real_secret

# --------------------------------------------------------
# n8n INTEGRATION (Phase 17+)
# --------------------------------------------------------
N8N_WEBHOOK_BASE_URL=
N8N_API_KEY=do_not_commit_real_secret

# --------------------------------------------------------
# ANTHROPIC / AI (Phase 7+)
# Get from: console.anthropic.com
# --------------------------------------------------------
ANTHROPIC_API_KEY=do_not_commit_real_secret

# --------------------------------------------------------
# CONTENT / DESIGN / VIDEO FACTORY — n8n AI Provider webhooks (Phase: Content Factory V1+)
# Set ONLY in Vercel/n8n environment. Leave blank to use the local fallback
# generator. The real webhook URLs and any AI provider keys must stay in n8n
# Credentials — never commit a real URL/secret and never expose AI keys here.
# --------------------------------------------------------
VITE_N8N_CONTENT_FACTORY_WEBHOOK_URL=
VITE_N8N_DESIGN_FACTORY_WEBHOOK_URL=
VITE_N8N_VIDEO_SCRIPTS_WEBHOOK_URL=

# --------------------------------------------------------
# APP IDENTITY (public, safe to commit if needed)
# --------------------------------------------------------
VITE_APP_NAME=The Core Agency
VITE_APP_TAGLINE=AI Marketing Team Workspace
VITE_APP_MODE=Real Operations MVP
